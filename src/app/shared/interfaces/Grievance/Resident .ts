export interface Resident {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
  isDeleted: boolean;
  isActive: boolean;
  // Add more fields if they exist, like name or email
}
